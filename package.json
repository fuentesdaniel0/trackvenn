{
  "name": "trackvenn",
  "version": "1.0.0",
  "description": "trackvenn monorepo",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && cross-env HOST=127.0.0.1 NODE_OPTIONS=--openssl-legacy-provider npm start",
    "postinstall": "cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "cross-env": "^10.1.0"
  }
}
